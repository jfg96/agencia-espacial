<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="agencia-espacial">
        <provider>com.objectdb.jpa.Provider</provider>

        <!--
            Ruta del fichero de base de datos local.
            El fichero se creará automáticamente en la carpeta data/
            relativa al directorio desde el que se ejecuta la aplicación.
            Añade data/ y *.odb a tu .gitignore.
        -->
        <properties>
            <property name="javax.persistence.jdbc.url"
                      value="data/agencia-espacial.odb"/>
        </properties>

    </persistence-unit>

</persistence>
